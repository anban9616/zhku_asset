<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="zhku.zhou.asset.dao.UserPageMapper">
	<select id="selectAll" resultType="zhku.zhou.asset.entity.UserPage">
		SELECT
			u.id,
			r.name role,
			o.name organization,
			u.name,
			u.pwd,
			u.sex,
			u.birth,
			u.phone,
			u.email,
			u.login_time loginTime,
			u.login_ip loginIp,
			u.crtm,
			u.mdtm
		FROM 	
			user u 
			LEFT OUTER JOIN role r ON u.`rid`=r.`id`
			LEFT OUTER JOIN organization o ON u.`ogid`=o.`id`
	</select>
</mapper>